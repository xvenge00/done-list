// import type { PageLoad } from "$types/src/routes";

// export load: PageLoad = async({ fetch, params }) {
//     const res = await fetch(`/api/items/${params.id}`);
//     const item = await res.json();

//     return { item };
// }